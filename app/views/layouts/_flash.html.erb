<% flash.each do |key, value| %>
    <div class="fixed-top container">
      <div class="alert <%= bs4_bgcolor_for(key) %> alert-dismissable fade show my-4">
        <%= value %>
        <button class="close" data-dismiss="alert">x</button>
      </div>
    </div>
<% end %>